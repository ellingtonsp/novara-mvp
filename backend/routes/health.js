/**
 * Health Check Routes
 * These are defined in main server.js for quick access
 */

const express = require('express');
const router = express.Router();

// Health routes are defined in main server.js for performance
// This file exists for completeness of the modular structure

module.exports = router;