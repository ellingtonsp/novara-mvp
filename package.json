{
  "name": "novara-mvp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci --only=production",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
} 